/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Server Actions are stable, but keep this empty so it doesn't break across versions.
  },
};

export default nextConfig;
